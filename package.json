{
  "name": "gotrip",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A travel-planning ReAct agent — reusable agent-development skill with trip planning as the carrier.",
  "scripts": {
    "dev": "tsx src/interface/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "openai": "^4.0.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
